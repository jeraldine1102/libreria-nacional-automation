serenity {
  project.name = "LPortafolio - Librería Nacional"
  test.root = com.jeral.portafolio
  webdriver {
    driver = edge
    autodownload = true
  }
  browser.maximize = true
  use.unique.browser = false
  restart.browser.each.scenario = true
}